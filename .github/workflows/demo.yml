name: Basic Workflow

on:
  push:
    branches:
      - main

jobs:
  simple-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print a message
        run: echo "GitHub Actions is running successfully!"
